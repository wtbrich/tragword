[tool.ruff]
line-length = 100
target-version = "py311"
src = ["app", "scripts", "tests", "ui"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
